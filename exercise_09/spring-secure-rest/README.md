# Spring Secure REST API

A simple Spring Boot application which serves a secured REST API. 

## Run

From your IDE or via the command line

```bash
$ ./mvnw spring-boot:run
# or
$ mvn spring-boot:run
```
